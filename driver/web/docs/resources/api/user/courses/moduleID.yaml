put:
  tags:
  - Client
  summary: Update custom user course
  description: |
    Update custom user course
  security:
    - bearerAuth: []
  parameters:
    - name: course-key
      in: path
      description: course Key
      required: true
      style: simple
      explode: false
      schema:
        type: string
    - name: module-key
      in: path
      description: Module Key
      required: true
      style: simple
      explode: false
      schema:
        type: string
  requestBody:
    description: updated unit
    content:
      application/json:
        schema:
          $ref: "../../../../schemas/custom/Unit.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: "../../../../schemas/custom/Unit.yaml"
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error
  x-core-function: UpdateUserCourseUnitProgress
  x-data-type: model.Unit
  x-authentication-type: User